env: ${NODE_ENV}
debug:
  port: ${DEBUG_PORT}
health:
  port: 9000
logger:
  elasticsearch:
    host: ${LOGGER_ELASTIC_HOST}
    level: ${LOGGER_ELASTIC_LEVEL}
database:
  uri: mongodb://
server:
  port: ${SERVER_PORT}
  headers:
    - teste
    - teste2